<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.winter.constraintlayout">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--相对定位-->
        <activity android:name=".RelativePositioningActivity">

        </activity>

        <!--角度定位-->
        <activity android:name=".AnglePositioningActivity">

        </activity>

        <!--边距-->
        <activity android:name=".MarginActivity">

        </activity>

        <!--尺寸约束-->
        <activity android:name=".SizeConstraintActivity">

        </activity>

        <!--链-->
        <activity android:name=".ChainActivity">

        </activity>

        <!--辅助工具-->
        <activity android:name=".AuxiliaryToolActivity">

        </activity>

    </application>

</manifest>